info:
  name: POST
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/fruits
  body:
    type: json
    data: |-
      {
          "name": "Яблуко",
          "brand": "Голден",
          "price_per_kg": 35.50,
          "stock_kg": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
