info:
  name: UpdatePrice
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/fruits/1/price
  body:
    type: json
    data: |-
      {
        "price": "42"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
