info:
  name: UpdateStock
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:3000/fruits/1/stock
  body:
    type: json
    data: |-
      {
        "stock": 400
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
